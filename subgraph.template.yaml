specVersion: 0.0.2
description: Decentraland TPR
#graft:
#  base: QmYf4h6nbvohbxUAXF6U3sVsNvQrZz5zkNgeh6Ah4S7vts # Subgraph ID of base subgraph
#  block: 18290388 # Block number
repository: https://github.com/decentraland/tpr-graph
schema:
  file: ./schema.graphql
dataSources:
  # ---------------------------------------------------------
  # Tiers ----------------------------------------------
  # ---------------------------------------------------------
  - kind: ethereum/contract
    name: Tier
    network: {{network}}
    source:
      address: '{{address_tiers}}'
      abi: Tier
      startBlock: {{startBlock_tiers}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      file: ./src/handlers/tiers.ts
      entities:
        - Tier
      abis:
        - name: Tier
          file: ./abis/Tiers.json
      eventHandlers:
        - event: TierAdded((uint256,uint256))
          handler: handleTierAdded
  # ---------------------------------------------------------
  # Third Party Registry --------------------------------------
  # ---------------------------------------------------------
  - kind: ethereum/contract
    name: ThirdPartyRegistry
    network: {{network}}
    source:
      abi: ThirdPartyRegistry
      startBlock: {{startBlock_tpr}}
      address: '{{address_tpr}}'
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      file: ./src/handlers/thirdparty.ts
      entities:
        - ThirdParty
        - Item
      abis:
        - name: ThirdPartyRegistry
          file: ./abis/ThirdPartyRegistry.json
      eventHandlers:
        - event: ThirdPartyAdded(string,string,string,bool,address[],address)
          handler: handleThirdPartyAdded
        - event: ThirdPartyItemsBought(string,uint256,uint256,address)
          handler: handleThirdPartyItemsBought
        - event: ItemAdded(string,string,string,bool,address)
          handler: handleItemAdded
        - event: ItemUpdated(string,string,string,address)
          handler: handleItemUpdated
